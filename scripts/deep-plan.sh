#!/bin/bash
# deep-plan: Spawn an isolated Opus planning session with high thinking
# Usage: deep-plan "Your planning prompt here"
# Called by the agent via sessions_spawn, not directly

echo "Deep planning tool is invoked via sessions_spawn, not bash."
echo "See TOOLS.md for usage."
exit 1
